export const STORAGE = {
    session: 'session',
    lang: 'lang',
    theme: {
        schema: 'theme::schema',
        fontSize: 'theme::fontSize',
        primaryColor: 'theme::primaryColor',
    },
} as const;
